export interface TalkToMeTheme {
  textColor: string;
  backgroundColor: string;
  backgroundColor2: string;
  loadingSpinnerColor: string;
  boxShadowColor: string;
  hoverColor: string;
  chatBackgroundColor: string;
  borderColor: string;
  avatarBackgroundColor: string;
}
